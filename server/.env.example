# MCP Server Configuration Example
# Copy this file to .env and adjust values as needed

# Server Port
PORT=3000

# Storage Type: 'json' or 'sqlite'
# JSON: Simple file-based storage (default, zero setup)
# SQLite: Database with full-text search capability
STORAGE_TYPE=json

# JSON Storage Path (used when STORAGE_TYPE=json)
JSON_STORAGE_PATH=./data/contexts.json

# SQLite Database Path (used when STORAGE_TYPE=sqlite)
# Database will be created automatically if it doesn't exist
SQLITE_DB_PATH=./data/mcp.db

# Session Timeout (milliseconds)
# Default: 7200000 (2 hours)
SESSION_TIMEOUT=7200000

# Environment
NODE_ENV=development
